# Test tree placeholder

Planned test responsibilities include:

- domain unit tests;
- application/use-case tests;
- real SQLite integration tests;
- architecture dependency tests;
- backup/restore and migration failure-path tests;
- selected WinForms accessibility/DPI smoke tests.

Avoid a test project per production class/library unless it has a useful isolation or runtime boundary.
