# Third-party notices — 0.5.0

SASD PIMS is licensed under Apache-2.0. Runtime dependencies are the Microsoft .NET/EF Core/Extensions packages (MIT) and SQLitePCLRaw (Apache-2.0) with the SQLite native library (public domain). The self-contained .NET distribution includes Microsoft's bundled third-party notices. Exact versions and the SPDX SBOM are included with the release artifact; see `DEPENDENCIES.md`.
