# Third-party notices — SASD PIMS 0.1.0

Direct NuGet dependencies are unchanged from `0.0.1-internal`:

- Microsoft.EntityFrameworkCore.Sqlite 10.0.10 — MIT — SQLite persistence;
- Microsoft.Extensions.Logging / Abstractions 10.0.10 — MIT — logging contracts and composition;
- SQLitePCLRaw.lib.e_sqlite3 2.1.12 — Apache-2.0 — pinned native SQLite runtime;
- xunit.v3 3.2.2 — Apache-2.0 — tests only.

The self-contained package also includes Microsoft .NET runtime components under their respective
Microsoft licenses. See package metadata and the repository `LICENSE` for details.
